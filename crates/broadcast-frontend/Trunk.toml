[build]
filehash = false

# Reachable from the local network. Nothing sensitive is served: these are
# the same static files a deploy publishes, and the page talks to MARA from
# the browser rather than through anything running here.
[serve]
addresses = ["0.0.0.0"]
